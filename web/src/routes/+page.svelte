<script>
	// Accueil = affiche interactive, UN SEUL ÉCRAN (révision 2026-07-18). C'est l'entrée
	// dans l'application : rien sous la couverture (pas de chiffre, pas de méthode, pas de
	// pied de page). Les chiffres détaillés vivent dans « Comprendre les mentions », le
	// calcul et les sources dans « Méthode ».
	import LandingCover from '$lib/LandingCover.svelte';
</script>

<LandingCover />
