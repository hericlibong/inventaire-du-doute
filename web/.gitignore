node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# Données synchronisées depuis le pipeline Python (générées, non versionnées).
# Les régénérer côté back puis lancer `npm run sync:data`.
/static/data

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
